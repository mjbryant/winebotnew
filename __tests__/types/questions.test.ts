import { TemplateWine } from "@/types/questions";
import { deepCopy } from "@/utils/helpers";

describe("TemplateWine", () => {
  it("returns 2 for a single exact match", () => {
    // Assert that TemplateWine has all attributes set. I have no idea how to do this.
    const t = deepCopy(TemplateWine);
  });
});
